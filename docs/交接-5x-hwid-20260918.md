# 5.10/5.15 hwid 校验版交接（2026-09-18）

> 本仓 = `oppo_oplus_realme_5x` 的 HWID 校验版。非 hwid 版 12/12 全绿（ccache 全存，复跑 10 分钟）。
> 本仓改动：Checkout 步骤 + HWID 注入步骤 + CCACHE_KEY 加 `-hwid` 后缀 + hwid/ 目录。

## 当前状态（2026-09-18 15:00）

- 最新 run：**首个验证 run 35316698050（5.10.236）在跑**——Checkout / 初始化 / 注入 HWID / ccache 步骤全部 success，编译中（首跑无 -hwid 缓存，预计 20 分钟量级）
- 挂在哪：无（目前各步骤全过）
- 已做修复：预检全部通过——① 12 个 yml YAML + bash -n 校验通过 ② 12 棵树注入锚点实测存在（`static int kernel_init(void *);` + `setup_command_line(command_line);` + `obj-y.*main.o`）③ 本地模拟注入成功（hwid_lock.o 进 Makefile、声明+调用进 main.c、allowlist.h 生成）④ hwid_lock.c 用 GKI-hwid 的 5-key 版（不含 panic.h，5.10/5.15 兼容；sm8650-hwid 的 2-key 版含 `#include <linux/panic.h>`，5.10 树无此头会编译失败）
- 下一步：35316698050 跑绿后 → 批量触发其余 11 个 x → 全绿后设备端真机验证（先确认设备在 allowlist）

## hwid_lock.c 选型（重要）

用 `GKI_KernelSU_SUSFS-hwid/hwid/hwid_lock.c`（5-key + hwid_match 宽容比对 + DRY_RUN），**不用** `oppo_oplus_realme_sm8650-hwid` 的 2-key 版：
- 2-key 版 `#include <linux/panic.h>` —— 5.10/5.15 树无此头（6.x 才拆分出来），会直接编译失败
- 5-key 版只用 `kernel.h`（panic/panic_timeout 在 5.10/5.15 的 kernel.h 里，已实测）

## 与非 hwid 版的全部差异（改造点）

1. `- name: Checkout repository` + `actions/checkout@v7`（首个 step；release job 不需要）
2. `- name: 注入 HWID 白名单校验`（插在「配置ccache目录」之前）：cp hwid 三件套 → `inject_hwid_lock.sh kernel_workspace/common`
3. 仓内资源从 wget 自身改为 `$GITHUB_WORKSPACE` 本地 cp（ccache-x86-64 / apk_sign.patch / zram_patch / config.patch / libfakestat.so / libfaketimeMT.so / zram.zip）
4. `CCACHE_KEY: ccache-ecsv4-<x>-hwid`（与非 hwid 仓缓存隔离）

## 注入点时序（已核对 start_kernel）

`setup_boot_config(command_line)` → `setup_command_line(command_line)` → **`hwid_lock_verify()`** → `setup_nr_cpu_ids()`...
即 bootconfig 和 cmdline 都就绪后才校验，`boot_command_line` 此时有效。

## 加设备流程

1. 设备端（原厂内核）跑 `sh hwid/probe_hwid.sh [预期id]` → `/sdcard/hwid_probe.log`
2. 取**原始值**（不要信识别脚本格式化输出），确认来源 key
3. 填 `hwid/allowlist.txt`（`^[0-9a-z._:-]{4,128}$`，超长 ID 必须原值逐字符一致）
4. 提交推送 → 触发构建 → 刷入前确认设备在 allowlist（不在会 panic 不开机）
